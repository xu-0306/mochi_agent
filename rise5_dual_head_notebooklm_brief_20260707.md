# Rise5 Dual-Head 標籤訓練簡報素材

日期：2026-07-07

## 給 NotebookLM 的使用說明

這份文件是為了生成簡報而整理。請把簡報重點放在 rise5 標籤設計、2head 訓練主線、4head 對照實驗，以及推論結果如何用於交易篩選。

建議簡報主軸如下：

1. 先說明原本 rise5 二分類標籤的限制。
2. 再說明新標籤如何把方向與風險拆開。
3. 強調目前主要訓練方向是 2head：做空一組、做多一組。
4. 4head 是補充對照，用來測試四個目標一起學是否有額外幫助。
5. 最後說明輸出結果應該用各 head 的機率做排序與篩選，而不是只看 accuracy。

簡報語氣建議：用白話說明，不要變成程式碼或模型架構教學。

---

## 1. 原本的 Rise5 標籤

原本 rise5 使用一個簡單二分類標籤。

計算方式：

```text
ret_forward = D9 close / D5 open - 1
```

標籤規則：

| 條件 | 原本標籤 |
|---|---|
| `ret_forward >= 0` | `1`，未來 5 天最後上漲 |
| `ret_forward < 0` | `0`，未來 5 天最後下跌 |

簡單說，舊標籤只問一件事：

> 從 D5 開盤進場，到 D9 收盤出場，最後是漲還是跌？

---

## 2. 舊標籤的問題

舊標籤雖然簡單，但對交易來說太粗。

例如以下兩種情況，在舊標籤裡都被當成 `1`：

- 未來 5 天漲 `+0.1%`
- 未來 5 天漲 `+25%`

但這兩種機會完全不同。

同樣地，以下兩種情況在舊標籤裡都被當成 `0`：

- 未來 5 天跌 `-0.1%`
- 未來 5 天跌 `-10%`

這會造成兩個問題：

1. 小漲小跌很可能只是雜訊，卻被硬切成不同類。
2. 大漲大跌才是交易上最重要的風險與機會，卻沒有被特別標出來。

---

## 3. Rise5 為什麼需要拆成方向與風險

Rise5 的樣本通常發生在股票已經上漲一段之後。

這時接下來 5 天可能有三種常見結果：

- 繼續大漲
- 小幅震盪
- 開始回檔

舊標籤只能把結果切成「最後漲」或「最後跌」，但交易真正關心的是：

- 如果做空，會不會中途被大漲軋到？
- 如果做多，會不會中途遇到大跌？
- 這是小幅波動，還是真正有報酬空間？

所以 rise5 的瓶頸不只是模型能力，而是原本的學習目標太模糊。

---

## 4. 新標籤的核心想法

新的標籤不再只問「最後有沒有漲」。

它把一筆樣本拆成四個交易問題：

| 新標籤 | 白話意思 | 模型要學什麼 |
|---|---|---|
| `short_good` | 適不適合做空 | 未來是否有明顯下跌 |
| `big_up_risk` | 做空會不會被軋 | 未來期間是否曾大漲 |
| `long_good` | 適不適合做多 | 未來是否有明顯上漲 |
| `big_down_risk` | 做多會不會遇到大跌 | 未來期間是否曾大跌 |

這四個標籤不是四選一，而是四個可以同時成立或不成立的問題。

這種設計叫做 multi-label learning。

---

## 5. 目前主要訓練方向：2head

目前除了 4head 對照組以外，主要訓練方向應該還是 2head。

原因是交易決策本身可以自然拆成兩條線：

| 訓練組 | heads | 用途 |
|---|---|---|
| 做空 2head | `short_good`、`big_up_risk` | 找做空機會，同時避開被軋風險 |
| 做多 2head | `long_good`、`big_down_risk` | 找做多機會，同時避開大跌風險 |

2head 的好處是任務比較集中。

做空模型專心回答：

```text
這筆適不適合做空？做空會不會被大漲軋？
```

做多模型專心回答：

```text
這筆適不適合做多？做多會不會遇到大跌？
```

這比把四個問題全部塞進同一個模型更容易解讀，也更貼近後續交易篩選。

---

## 6. 4head 的定位

4head 不是目前唯一主線，而是補充對照。

4head 同時學四個目標：

- `short_good`
- `big_up_risk`
- `long_good`
- `big_down_risk`

它的用途是測試：

- 四個目標一起學，是否能共享特徵。
- 做多與做空訊號是否互相幫助。
- 多任務訓練是否比 2head 更穩定。

但在解讀結果時，仍應該把 2head 當成主要版本，4head 當成比較組。

---

## 7. 四個新標籤怎麼算

新資料除了看最後收盤報酬，也看未來 5 天期間的最高價和最低價。

核心欄位：

| 欄位 | 意思 |
|---|---|
| `future_5d_return` | `D9 close / D5 open - 1` |
| `future_5d_max_up_return` | `D5~D9` 期間最高價相對 `D5 open` 的最大上漲 |
| `future_5d_max_down_return` | `D5~D9` 期間最低價相對 `D5 open` 的最大下跌 |

四個 head 的直覺門檻：

| 新標籤 | 大致條件 |
|---|---|
| `short_good` | 最後報酬跌超過約 `2%` |
| `big_up_risk` | 期間曾上漲超過約 `5%` |
| `long_good` | 最後報酬漲超過約 `2%` |
| `big_down_risk` | 期間曾下跌超過約 `5%` |

這樣做的好處是：模型不只學最後結果，也學交易過程中的風險。

---

## 8. 一個直覺例子

假設某一筆樣本：

- `D5 open = 100`
- 未來 5 天最高漲到 `108`
- 未來 5 天最低跌到 `96`
- `D9 close = 97`

最後結果是：

```text
future_5d_return = -3%
```

如果只用舊標籤，這筆會被標成「下跌」。

但新標籤會提供更多資訊：

| 標籤 | 結果 | 解讀 |
|---|---:|---|
| `short_good` | `1` | 最後跌超過 2%，做空方向正確 |
| `big_up_risk` | `1` | 中途曾漲到 +8%，做空有被軋風險 |
| `long_good` | `0` | 最後沒有明顯上漲 |
| `big_down_risk` | `0` | 中途沒有跌超過 5% |

這筆交易很適合說明新標籤的價值：

> 舊標籤只說最後做空賺錢；新標籤會提醒，這筆做空過程中其實很危險。

---

## 9. 新模型輸出的意義

舊模型只需要輸出一個機率：

> 未來 5 天最後上漲的機率。

新模型會輸出各 head 的機率。

做空 2head 輸出：

- `P(short_good)`
- `P(big_up_risk)`

做多 2head 輸出：

- `P(long_good)`
- `P(big_down_risk)`

4head 則同時輸出四個機率。

這些機率可以用來做排序與篩選，而不是只拿來做 0/1 分類。

---

## 10. 對交易決策的意義

新標籤可以讓模型從「分類器」更接近「交易篩選器」。

做空時，不只看：

```text
P(short_good) 高不高
```

還要看：

```text
P(big_up_risk) 低不低
```

也就是說，理想的做空候選不只是「可能下跌」，還要是「不太容易被大漲軋」。

做多也類似：

```text
P(long_good) 高，且 P(big_down_risk) 低
```

這比單純判斷漲跌更貼近實際交易。

---

## 11. 目前資料狀態

正式產出的 rise5 dual-head 標籤資料已包含 train 和 test。

四個新 head 的正類比例大約落在 `28%~42%`，不是極端少數事件。

| 標籤 | train 正類比例 | test 正類比例 |
|---|---:|---:|
| `short_good` | 約 `39.9%` | 約 `42.3%` |
| `big_up_risk` | 約 `38.8%` | 約 `39.1%` |
| `long_good` | 約 `30.3%` | 約 `28.7%` |
| `big_down_risk` | 約 `37.6%` | 約 `42.4%` |

這代表第一版可以先用標準 BCE loss 訓練，不需要一開始就用很複雜的 focal loss。

---

## 12. 建議的實驗對照

為了驗證新標籤是否真的有效，建議保留 12 組對照。

主要組是 2head：

| 類型 | 版本 |
|---|---|
| 原版圖片，做空 2head | base、risk-aware |
| 原版圖片，做多 2head | base、risk-aware |
| split_channel 圖片，做空 2head | base、risk-aware |
| split_channel 圖片，做多 2head | base、risk-aware |

補充比較組是 4head：

| 類型 | 版本 |
|---|---|
| 原版圖片，4head | base、risk-aware |
| split_channel 圖片，4head | base、risk-aware |

這樣的設計可以同時回答兩件事：

1. 對 rise5 來說，2head 是否已經足夠。
2. 4head 是否真的帶來額外價值。

---

## 13. 評估方式要跟交易目標一致

這個任務不應只看 accuracy。

原因是：accuracy 只看分類對不對，但交易上更重要的是篩出來的候選是否真的有價值。

建議評估：

- 做空候選的平均未來報酬是否更低。
- 做空候選是否避開 `big_up_risk`。
- 做多候選的平均未來報酬是否更高。
- 做多候選是否避開 `big_down_risk`。
- 排名前 10% 或 20% 的樣本是否比全體樣本更有交易價值。

也就是說，模型分數要能用來排序與篩選，而不只是讓分類報表好看。

---

## 14. 輸出檔案應該如何理解

每個訓練組都應該分開保存 train 和 test 的推論結果。

建議結構：

```text
runs_clean/<run_name>/
  train/
    predict.csv
    label_dual_head.txt
    metrics_multilabel.json
    *_classification_report.txt
    *_confusion_matrix.png
    *_roc_curve.png
  test/
    predict.csv
    label_dual_head.txt
    metrics_multilabel.json
    *_classification_report.txt
    *_confusion_matrix.png
    *_roc_curve.png
  best_multilabel.pth
  run_config.json
  training_summary.json
  metrics_by_head.csv
```

`predict.csv` 不需要輸出原始價格欄位，也不需要輸出所有中間計算欄位。

它的重點應該是：

- `filename`
- 各 head 的真實 label
- 各 head 的預測類別
- 各 head 的 class 0 / class 1 機率

---

## 15. 簡報總結

本專案的核心不是單純做一個漲跌分類器。

它的完整邏輯是：

1. 發現原本 rise5 的 0/1 標籤太粗，無法表達交易風險。
2. 改用方向與風險拆開的新標籤。
3. 目前主要訓練方向是 2head：做空一組、做多一組。
4. 4head 作為補充比較，用來確認四個目標一起學是否更好。
5. 最終目標是讓模型成為交易候選篩選器，而不是只做表面上的漲跌預測。

一句話總結：

> 舊標籤問「5 天後有沒有漲」；新標籤問「這筆交易值不值得做，以及過程中會不會有大風險」。

---

## 16. 建議投影片標題

1. 原本 Rise5 標籤：只看 5 天後漲跌
2. 為什麼舊標籤太粗
3. Rise5 的真正難點：方向與風險混在一起
4. 新標籤設計：四個交易問題
5. 目前主線：做空 2head 與做多 2head
6. 4head 的定位：補充對照，不是唯一主線
7. 四個 head 的定義
8. 例子：最後賺錢但中途風險很大
9. 模型輸出如何轉成交易篩選
10. 實驗設計：原版、split_channel、2head、4head
11. 評估方式：用排序與交易價值看結果
12. 輸出檔案與可複現資訊
13. 總結：從漲跌分類到交易風險學習
